
# Ścieżka do credentials (już masz)
SHARED_CREDENTIALS_FILE=${AWS_SHARED_CREDENTIALS_FILE:="${HOME}/.aws/credentials"}

# Unikalny bucket (już masz)
UNIQUE_BUCKET_STRING=${UNIQUE_BUCKET_STRING:="ldsecond-c15a9"}

# Ustaw nazwę grupy logów na coś innego, żeby uniknąć konfliktu
LOG_GROUP_NAME="/aws/eks/backend-eks/custom-cluster-$(date +%s)"

# Eksportuj dla Terraform
export TF_VAR_cloudwatch_log_group_name="$LOG_GROUP_NAME"
export TF_VAR_create_cloudwatch_log_group=false

echo "Używam CloudWatch Log Group: $LOG_GROUP_NAME"
